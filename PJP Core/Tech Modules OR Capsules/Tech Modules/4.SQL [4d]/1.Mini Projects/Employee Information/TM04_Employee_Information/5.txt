a)
SELECT EMPNO, FIRST_NAME || ' ' || LAST_NAME "EMPLOYEE"
FROM EMP_DETAILS
WHERE EMPNO=(SELECT EMPNO FROM EMP_SKILL WHERE UPPER(SKILL)=UPPER('sybase'));

b)
SELECT FIRST_NAME, SKILL, LODGE_MANAGER
FROM EMP_DETAILS NATURAL JOIN EMP_SKILL NATURAL JOIN LODGE_DETAILS;

c)
SELECT 101 "EMPNO", COUNT(SKILL) "NO. OF SKILLS" 
FROM EMP_SKILL
WHERE EMPNO=101;

d)
SELECT FIRST_NAME 
FROM EMP_DETAILS
WHERE EMPNO=(SELECT EMPNO FROM EMP_SKILL WHERE SKILL='Oracle SQL');
