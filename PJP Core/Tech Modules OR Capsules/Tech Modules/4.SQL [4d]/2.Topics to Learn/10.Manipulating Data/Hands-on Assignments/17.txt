MERGE INTO EMP2 T
USING EMP E
ON (T.EMPNO=7788)
WHEN MATCHED THEN UPDATE SET ENAME=E.ENAME, SAL=E.SAL
WHEN NOT MATCHED THEN INSERT(EMPNO, ENAME, SAL) VALUES(E.EMPNO, E.ENAME, E.SAL) WHERE E.SAL>3000;
