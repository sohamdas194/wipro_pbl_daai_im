CREATE TABLE EMP1
(
EMPNO INTEGER CONSTRAINT EMP1_PK PRIMARY KEY,
ENAME VARCHAR2(20) NOT NULL,
SAL NUMBER(10, 2) CONSTRAINT EMP1_SAL_CK CHECK (SAL > 5000),
MGR NUMBER REFERENCES EMP1(EMPNO),
DEPTNO INTEGER REFERENCES DEPT1(DEPTNO)
);
